# 2026-09-09 — CNB 公开仓镜像到 GitHub（思思）

兔兔需求：把 CNB 对外公开的两个仓库同步到 GitHub（github.com/qitu72）。查证：CNB 无原生"推送即镜像 GitHub"开关，走**本地双遥控 + 一键脚本**。

## 布置

| CNB 仓库 | 本地克隆 | GitHub 镜像 |
|---|---|---|
| cnb.cool/72boom/dsh-plugins | `D:\aolong\repos\dsh-plugins` | github.com/qitu72/dsh-plugins |
| cnb.cool/72boom/72skill | `D:\aolong\repos\72skill` | github.com/qitu72/72skill |

- 两仓已加第二遥控 `github`（origin=CNB 不动，日常收工推送流程零变化）
- 一键脚本：`C:\Users\七兔\scripts\sync-cnb-to-github.ps1`（UTF-8 BOM；外网预检 8 秒 → 逐仓 push `--all --tags` ×3 重试 → 中文总结；`http.postBuffer 500MB + HTTP/1.1` 弱网参数）
- **触发方式：纯手动**（需 github.com 可达即可——有时内网直连也通；不挂 automation，避免与 DSH 任务抢网络）。

## 兔兔一次性前置（GitHub 网页，中文坐标）

1. 建两个**空仓库**（不勾 README/.gitignore/license）：github.com → 右上 + → New repository → 名字分别 `dsh-plugins` / `72skill`
2. 建 PAT（首次推送凭据）：头像 → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token (classic) → 勾 `repo` → 生成后复制 `ghp_` 串（仅显示一次）

## 首推与日常

- 首推：开外网 → 跑脚本 → git 询问时 Username=`qitu72`、Password=粘贴 PAT（凭据管理器记住，此后免输）
- 日常：CNB 收工推送照旧（不经外网）；想更新 GitHub 镜像时开外网跑一次脚本（30 秒）
- 断联无损：git 断点续传，失败重跑脚本即可；本地/CNB 数据零风险

## 首推实录（09-09，已完成 ✓）

- 首次推送被拒：GitHub 建仓时带了自动初始化的 "Initial commit"（单 README）→ 非快进拒绝。
- 处置：fetch 核验远端仅此一件无价值内容 → `--force` 镜像覆盖 → GitHub main 与 CNB main 完全一致（dsh-plugins=9ed4e5d、72skill=b8c1856；dsh-plugins 附带历史分支 feat/init-skill-sync 一并镜像）。
- 凭据：GCM 弹窗一次，PAT 已入 Windows 凭据管理器，此后免输。
- **脚本修正**：原版退出码取了 `--tags` 那条（`--all` 被拒时误报成功=假绿）→ 现两条推送分别记 `$rc1/$rc2` 都为 0 才算成功；推送改为 `--force` 镜像语义（GitHub 仅作只读镜像，以 CNB 为准），从根上杜绝 fetch-first 误报。
- GITHUB 上可看：github.com/qitu72/dsh-plugins · github.com/qitu72/72skill

## 令牌换绑（09-09，已完成 ✓）

兔兔按首轮指引建了 90 天令牌，后按建议又生成了 No expiration 令牌——凭据管理器里旧令牌静默生效（git 不再弹窗属正常行为）。换绑工具：`C:\Users\七兔\scripts\update-github-token.ps1`（SecureString 掩码粘贴 → `git credential approve` 原地覆盖 → 自动跑一次 sync 真实验证）。兔兔实测：新令牌写入 ✓ 双仓推送成功 2/失败 0 ✓ 旧 90 天令牌已在 GitHub 网页删除 ✓。**现状：唯一有效凭据 = No expiration 令牌（仅 repo 权限），存于凭据管理器；令牌本身不经聊天记录、不落文件。** 观察备注：验证推送出现过两次 forced update（远端曾被推到未知 commit 932e557/c67d51a 后被镜像纠回）——若系网页端试改文件所致属正常镜像纠偏；提醒兔兔：改动请走 CNB 侧，GitHub 仅作展示镜像。
