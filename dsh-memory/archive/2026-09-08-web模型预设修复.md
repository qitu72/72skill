# 2026-09-08 Web(3080) 模型预设消失 — 诊断与修复全记录

> 执行窗口：DSH 桌面端主窗口（工作区 D:\aolong）；修复经兔兔确认后执行。

## 一、现象

- Web 端 `http://127.0.0.1:3080/`（独立 `dsh web` 实例 = `--profile web`）模型选择器只剩 DeepSeek 系（V4-Flash/Pro/Vision-Exp + modlens vision 组），**zai（glm-5.3-flash/glm-5.3）、glm-coding、qianfan（ERNIE-5.1）三个自定义供应商全部消失**
- 恢复历史会话报「当前模型不可用，请先选择模型」（该会话今晨还在用 zai/glm-5.3-flash 跑 1.3M tok 大会话）
- 桌面端（DSH Desktop 2.0.5，`--profile desktop`，GUI 43120）看起来正常 → 兔兔报告「两端不同步」

## 二、时间线（根因定位关键）

- 09-07 23:15 web 实例重启（`relaunch-web.ps1`，runtime rc.8→0.1.2-rc.1 已于 9/6 切换）
- 09-07 23:41-43 **DSH Desktop 首次启动**；23:42 一次 pnpm 操作（pid 7568）**中断**，把整套 @deepseek-ai 核心（约 230 包，含 cordis 家族）灌进 `profiles/web/node_modules`，留下 `_tmp_7568_*` 残留与 `dsh-credentials.bak-rc6`，其中 **`dsh-settings` 卡在旧版 0.1.0-rc.6**
- 09-08 上午 web 仍正常（zai 大会话 07:41-09:19，即昨晚日志损坏修复那个会话）
- 09-08 16:15 settings.yaml 被改（新增 `llm-deepseek` 模型段、zai 模型条目扩展 `input: [text,image]`）→ web 旧进程热重载拒绝；桌面端进程（23:41 启动）未重启，一直端着旧路由（**假活**）
- 09-08 傍晚兔兔发现 web 预设消失

## 三、排查与排除项（证据链）

1. 两端共享 `~/.dsh/settings.yaml` 与 `.credentials.yaml`；`dsh web` = `--profile web` 硬编码别名，profile 只换插件组合层，DSH_HOME 均为 `~/.dsh`
2. 两套运行时（`~/.dsh/runtime-0.1.2-rc.1` vs 桌面版 app.asar.unpacked）核心包哈希对比：`dsh-llm-pi-ai`/`dsh-llm`/`dsh-settings-file`/`dsh-credentials-local`/`dsh-home-paths` **逐字节相同**；仅 `dsh-settings` 桌面版多向后兼容垫片（不影响 installSection 语义）、`dsh-web-app` 构建差异
3. 探针实测（复制模块 + 追加 export 调内部函数）：当前配置对 runtime 正版代码 **Config schema 校验通过 + assertServiceable 深检通过** → 配置本身无辜
4. 哈希对比 web profile 内被灌入的核心副本：与 runtime 几乎全部相同，**唯 `dsh-settings` = rc.6 旧版**（18E5DD… vs 正版 BB4BEE…）
5. 对照组 desktop profile 的 `@deepseek-ai` 仅 5 个插件依赖包（cosmokit/dsh-credentials/dsh-settings/dsh-tools/schemastery）——web 被灌包前也应是这个形状

**根因**：profile 内残缺核心副本（rc.6 接缝包 + 中断残留）干扰核心链路模块解析，settings→llm-pi-ai 的 section 投递在 web 实例**静默断裂**（stderr 无痕，启动日志干净）；重启不修复（每次启动都加载被污染的 profile 树）。

## 四、修复（兔兔确认后执行）

1. 静态清单脚本 `D:\aolong\tmp\web-nm-cleanup.ps1`：`profiles/web/node_modules/@deepseek-ai` 下 230 个目录（保留上述 5 个依赖包）→ **Move-Item 移入** `~/.dsh/backups/web-profile-nm-cleanup-20260908/@deepseek-ai/`（非删除，可整体退回）
2. 重启 3080（`relaunch-web.ps1`，新 pid 50528，current-web-pid.txt 已正确更新）
3. 实测验证：模型列表 **zai / GLM Coding / Baidu ERNIE / DeepSeek 全组恢复**（连 `deepseek-v4.1-flash-expires-on-0910` 都回来了，反证此前配置确未完整应用）

## 五、遗留与后续（2026-09-08 晚洁癖收尾后更新）

- [ ] **桌面端择机重启**刷新模型路由（现为旧路由假活；若新配置对桌面也有影响，重启后才见真章）
- [x] ~~探针临时文件清理~~ → 已按回收站流程清理 4 件（runtime 的 `index.probe.mjs`、tmp 的 `check-llm-pi-ai.mjs`/`check-serviceable.mjs`/`web-nm-cleanup.ps1`，兔兔确认后执行）；静态清单的可审计副本在本 archive §四＋备份目录＋会话底账
- [x] ~~`profiles/web/node_modules` 顶层 `*_tmp_38940_*` 残留~~ → 30/30 逐个核验正式包在位后全部回收（desktop 对照组本就为 0）
- [x] ~~pnpm 台账一致性~~ → 只读体检 `pnpm ls --depth 0` exit 0，18 依赖全部可读，台账自洽（未跑 install，无需重建）
- [ ] 备份目录 `~/.dsh/backups/web-profile-nm-cleanup-20260908/`（230 项）观察一周无异常后可清
